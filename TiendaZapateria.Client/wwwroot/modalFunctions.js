window.modalFunctions = {
    mostrarModal: function (modalId) {
        $('#' + modalId).modal('show');
    },
    cerrarModal: function (modalId) {
        $('#' + modalId).modal('hide');
    }
};


